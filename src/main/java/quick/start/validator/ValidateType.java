package quick.start.validator;

public enum ValidateType {
     BYTE,
     SHORT,
     INTEGER,
     LONG,
     BIGINTEGER,
     FLOAT,
     DOUBLE,
     BIGDECIMAL,
     STRING,
     NUMBER,
     DATE,
     EMAIL,
     URL,
     EMPTY,
     NOT_EMPTY,
     BOOLEAN,
     PHONE,
     MOBILE,
     IP
}
